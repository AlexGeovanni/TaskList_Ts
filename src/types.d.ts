

// creamos un archivo con las interfaz que siempre vamos a utilizar para no estar creando codgo que al final no haran nada
export interface Items {
    id:string
    descrip:string
    isActive:boolean
}